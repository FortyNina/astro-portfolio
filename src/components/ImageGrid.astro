---
import { Image } from "astro:assets";
const { images, imageOrientation = "landscape" } = Astro.props;
---

<div class={`image-grid image-grid--${imageOrientation}`}>
  {images.map((img: ImageMetadata) => (
    <Image src={img} alt="" />
  ))}
</div>
