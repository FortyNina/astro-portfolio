---
import { Image } from "astro:assets";
const { images } = Astro.props;

---
<div class="image-grid">
  {images.map((img: ImageMetadata) => (
    <Image src={img} alt="" class="grid-img" />
  ))}
</div>
